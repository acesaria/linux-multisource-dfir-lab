# DFIR lab agent entry

Read `ai/CONTEXT.md`, select one task card and one stage, then load only their
named inputs. Read `ai/RULES.md` for autonomy and KISS constraints.
Current user instructions take precedence over saved project guidance.
If `ai/` is missing in a worktree, obtain the local ICM files before editing;
do not substitute historical reports or remembered metric contracts.
