# 04 Docs

## Problem

Write simple, factual project/scenario documentation from validated stage 02/03
outputs. No speculative claims; no absorbing investigation notebooks or
`runme.sh` analytical output into generic README text.

## Current state

- No stage-04 handoff written yet. Allowed targets: root `README.md`, module
  descriptions, scenario overview docs, simple usage/structure docs.

## Inputs

Per `ai/IDENTITY.md` read order, plus `ai/_config/{conventions,scope,done}.md`,
the relevant validated outputs from `ai/02_experiments/output/` and
`ai/03_investigation/output/`, and the README/doc file being updated.

## Next steps

- Update docs directly in the repository, aligned with validated outputs.
- Write a handoff to `ai/04_docs/output/summary.md` (docs updated, source
  outputs used, claims intentionally avoided, recommended thesis inputs).
